name: Workload Format Checker
on: [workflow_dispatch, pull_request]

jobs:
  custom-jinja2-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          git clone https://github.com/aristanetworks/j2lint.git && cd jtlint
          python3 -m pip install -e .
          j2lint --version
          j2lint --help

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v35

      - name: Run J2lint on changed JSON files in a pull-request
        if: github.event_name == 'pull_request'
        run: |
          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            if [[ $file == *.json ]]; then
              echo "Checking $file"
              j2lint "$GITHUB_WORKSPACE/$file" --extensions json --json
            fi
          done

      - name: Run J2lint on all JSON files in a branch
        if: github.event_name == 'workflow_dispatch'
        run: |
          for file in $(find . -name '*.json'); do
            echo "Checking $file"
            j2lint "$file" --extensions json --json
          done